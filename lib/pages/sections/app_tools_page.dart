
import 'package:flutter/material.dart';

class AppToolsPage extends StatelessWidget {
  const AppToolsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('App Tools'));
  }
}