
import 'package:flutter/material.dart';

class InsertDataPage extends StatelessWidget {
  const InsertDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Insert Data'));
  }
}
